name: Refresh Weather Data

on:
  schedule:
    - cron: '0 5 * * *'   # Every day at 5:00 UTC
  workflow_dispatch:        # Allow manual trigger

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15     # A healthy run takes ~2 min; fail fast instead of burning the 6h default
    permissions:
      contents: write       # Required to push the updated data.json

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0    # Full history avoids non-fast-forward push failures

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch weather data
        run: python scripts/fetch_weather.py

      - name: Commit updated data
        env:
          GIT_TERMINAL_PROMPT: 0   # Fail loudly instead of blocking on a credential prompt
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data.json
          git diff --staged --quiet || git commit -m "chore: refresh weather data [skip ci]"
          git pull --rebase origin main
          git push
